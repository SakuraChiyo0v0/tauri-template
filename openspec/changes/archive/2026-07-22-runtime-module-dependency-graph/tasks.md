## 1. 依赖协议与共享类型

- [x] 1.1 先扩展 Rust 与 TypeScript 清单测试，覆盖空依赖兼容、必需/可选依赖、自依赖、重复依赖和非法版本范围
- [x] 1.2 实现依赖清单解析，并扩展安装模块、计划、诊断、操作结果和影响类型
- [x] 1.3 更新最小示例和打包文档，说明运行时依赖与打包内依赖的边界

## 2. 依赖图解析器

- [x] 2.1 先编写纯 Rust 解析器测试，覆盖缺失、版本冲突、可选依赖、循环、稳定拓扑顺序和独立子图
- [x] 2.2 编写协调升级与确定性有限回溯测试，覆盖 A1/B1 到 A2/B2 和无解保留旧计划
- [x] 2.3 实现候选索引、版本范围剪枝、有限回溯、循环检测、拓扑排序和结构化诊断
- [x] 2.4 增加搜索上限与确定性结果测试，确保复杂或不可解输入返回诊断而不阻塞启动

## 3. 全局激活计划与迁移

- [x] 3.1 先编写计划存储测试，覆盖原子写入、单调代次、版本落盘后计划未提交和损坏计划
- [x] 3.2 先编写 V1 迁移测试，覆盖沿用活动版本、吸收旧禁用列表、重复迁移和损坏模块隔离
- [x] 3.3 实现全局计划读写、模块目录状态兼容层和 V1 首次迁移
- [x] 3.4 实现计划查询结果，聚合所有已安装模块、选择版本、等待诊断、拓扑顺序、依赖和依赖者

## 4. 依赖感知生命周期事务

- [x] 4.1 先编写安装事务测试，覆盖等待依赖、依赖到达后激活、协调升级、重复版本和旧计划保持
- [x] 4.2 改造安装流程为“版本落盘 → 求解 → 原子提交计划”，并返回计划是否改变
- [x] 4.3 先编写启用/停用测试，覆盖等待启用、成功启用、无依赖者停用和 provider 停用拒绝
- [x] 4.4 实现运行时模块启用/停用命令和结构化依赖影响错误
- [x] 4.5 先编写回滚/卸载测试，覆盖安全回滚、破坏依赖拒绝、依赖者阻止卸载和无依赖者卸载
- [x] 4.6 实现依赖安全回滚、卸载和激活失败后的重新求解

## 5. 前端计划消费与失败隔离

- [x] 5.1 先扩展加载器测试，覆盖拓扑激活、等待模块不执行、provider 失败传播和独立分支继续
- [x] 5.2 扩展运行时模块 API 与启动引导，迁移旧禁用状态并消费 Rust 全局计划
- [x] 5.3 重构发现与激活流程，严格按计划顺序注册贡献并隔离上游失败
- [x] 5.4 将运行时启停切换到 Rust 计划，保留内置源码模块现有状态行为

## 6. 模块管理体验与说明

- [x] 6.1 先扩展管理状态测试，覆盖等待依赖、版本冲突、循环、依赖者和操作可用性
- [x] 6.2 更新模块管理页面，展示必需/可选依赖、依赖者、选择/等待版本和结构化阻塞原因
- [x] 6.3 接入依赖感知启停、安装、回滚和卸载反馈，仅在计划代次变化时刷新 WebView
- [x] 6.4 更新 AI recipe、README 和示例清单，加入依赖开发和验证流程

## 7. 验证与归档

- [x] 7.1 运行前端测试、类型检查、生产构建、Rust 格式检查、Rust 测试和 Clippy
- [x] 7.2 使用真实多模块 `.mtp` 验证等待依赖、依赖到达、协调升级、阻止停用/卸载及安全回滚
- [x] 7.3 使用旧 V1 应用数据完成迁移冒烟验证，并确认无依赖模块行为不变
- [x] 7.4 对照规格完成静态审查，运行完整 Tauri 构建并严格验证 OpenSpec
- [x] 7.5 同步主规格并归档 `runtime-module-dependency-graph`
