## 1. 版本工具测试

- [x] 1.1 为版本输入解析、递增校验和版本文件同步编写失败测试
- [x] 1.2 为版本漂移、缺少 changelog 条目和无效目标编写失败测试

## 2. 版本维护实现

- [x] 2.1 实现无额外依赖的版本检查与升级脚本，并在 `package.json` 注册命令
- [x] 2.2 新增 `CHANGELOG.md` 并记录 `0.1.0` 与 `0.2.0` 的重要变化
- [x] 2.3 将底座版本同步提升到 `0.2.0`，更新示例模块的底座兼容范围
- [x] 2.4 在 README 中记录版本选择和发布顺序

## 3. 验证与归档

- [x] 3.1 运行版本工具测试、`version:check`、前端检查和 Rust 检查
- [x] 3.2 运行 Tauri 发布构建并核对生成产物版本
- [x] 3.3 严格校验 OpenSpec、同步主规格并归档变更
