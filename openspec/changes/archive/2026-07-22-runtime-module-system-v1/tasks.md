## 1. 模块协议与依赖

- [x] 1.1 先编写 Rust 与 TypeScript 清单解析测试，覆盖有效清单、非法标识、版本不兼容和贡献冲突
- [x] 1.2 定义 V1 模块清单、安装状态、前端运行时描述和宿主 SDK 类型
- [x] 1.3 加入 ZIP、语义版本、哈希、原子写入和 Tauri 文件对话框所需依赖与权限

## 2. Rust 模块包生命周期

- [x] 2.1 先编写安装服务失败测试，覆盖路径穿越、缺失入口、重复/较低版本和校验失败不切换
- [x] 2.2 实现包限制、清单校验、安全暂存、SHA-256 记录和活动状态原子写入
- [x] 2.3 先编写升级、显式回滚、激活失败恢复、首次激活受阻和卸载测试
- [x] 2.4 实现列出、安装、读取入口、报告激活失败、回滚和卸载 Tauri 命令
- [x] 2.5 注册模块管理命令并使用临时应用数据目录完成 Rust 集成测试

## 3. 动态注册表与运行时加载

- [x] 3.1 先扩展注册表测试，覆盖来源标记、动态注销、订阅通知和冲突隔离
- [x] 3.2 重构模块注册表为可观察聚合层，同时保持内置系统和日志模块行为
- [x] 3.3 先编写运行时清单转换、入口激活、元素缺失和失败回滚协调测试
- [x] 3.4 实现运行时模块发现、通用 Web Component 页面包装器和单文件 ESM 加载器
- [x] 3.5 实现 V1 宿主 SDK 的模块日志、命名空间设置及主题订阅
- [x] 3.6 调整应用启动引导，使运行时模块在首屏前完成发现并在失败时保持底座可用

## 4. 模块管理体验

- [x] 4.1 先编写模块管理状态与操作可用性测试
- [x] 4.2 扩展模块管理页面，展示内置/运行时来源、版本、错误及安装、启停、回滚、卸载操作
- [x] 4.3 接入 `.mtp` 文件选择、操作反馈和成功后的受控 WebView 重新加载
- [x] 4.4 提供未预装的最小示例模块源码、打包脚本和 AI 模块开发说明

## 5. 验证与交付

- [x] 5.1 运行前端测试、类型检查、生产构建、Rust 格式检查和 Rust 测试
- [x] 5.2 使用真实 `.mtp` 包验证首次安装、升级、失败不破坏、回滚、停用和卸载路径
- [x] 5.3 对照规格完成静态审查，修复场景覆盖、错误路径和范围漂移
- [x] 5.4 运行 `openspec validate runtime-module-system-v1 --strict`，归档变更并严格验证主规格
