## 1. Rust SQLite 契约测试

- [x] 1.1 先为模块路径隔离、持久化和同名表互不影响编写失败测试
- [x] 1.2 先为参数转换、只读查询、事务提交与回滚、schema 用户版本编写失败测试
- [x] 1.3 先为 ATTACH/DETACH、PRAGMA、多语句、越界结果和危险模块 ID 拒绝编写失败测试
- [x] 1.4 先为数据占用清单、活动模块清理拒绝及停用/孤立数据清理编写失败测试

## 2. Rust 底座实现

- [x] 2.1 引入受控 SQLite 依赖并实现按模块 ID 隔离的数据库管理器
- [x] 2.2 实现 SQLite authorizer、单语句参数化执行、只读查询和资源上限
- [x] 2.3 实现事务、schema 用户版本、数据占用统计和精确清理
- [x] 2.4 注册运行时数据库与数据清单 Tauri 命令，并验证模块 SDK 版本及激活状态

## 3. Host SDK V2

- [x] 3.1 先为清单 SDK 1/2 兼容、V1 无数据库成员和 V2 数据库调用编写失败测试
- [x] 3.2 扩展 Rust 与前端清单解析以接受 SDK 1/2 并拒绝其他版本
- [x] 3.3 实现数据库内部 API、Host SDK V1/V2 判别类型和按清单版本构造
- [x] 3.4 验证 V1 模块加载无回归，V2 模块能够通过 Host SDK 完成真实数据库操作

## 4. 模块数据管理体验

- [x] 4.1 为数据清单合并、容量格式化、活动模块保护和孤立数据清理编写前端测试
- [x] 4.2 在模块管理页展示数据库占用、保留数据条目和明确确认的清理操作
- [x] 4.3 更新独立 `tauri-module-template` 为 Host SDK V2，并增加数据库迁移与读写验证页面
- [x] 4.4 更新 README、AI 模块开发指引和 Unreleased 变更记录

## 5. 集成与验证

- [x] 5.1 使用独立模板生成真实 V2 `.mtp`，验证安装、激活、SQLite 写入、重启读取、停用、卸载保留和清理
- [x] 5.2 运行底座 `pnpm check`、Rust 测试、clippy 及相关构建验证
- [x] 5.3 对照规格完成安全与兼容性 review，运行 `git diff --check` 和 OpenSpec strict validate
- [x] 5.4 同步 CodeGraph，并记录模块服务层、配额、导入导出和强沙箱仍不在本次范围
