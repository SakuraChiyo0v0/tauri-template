## 1. 锁定协议与失败行为

- [x] 1.1 为 Rust 清单解析和前端清单解析增加 Host SDK V6 红灯测试，证明 V6 可声明仓库计划能力且 V2–V5 行为不变
- [x] 1.2 为仓库候选求解增加红灯测试，覆盖传递必需依赖、保持当前兼容版本、兼容升级、缺包、循环、自动降级拒绝和同版本内容冲突
- [x] 1.3 为计划会话增加红灯测试，覆盖会话与 grant 归属、计划代次变化、包替换和过期 `planId`
- [x] 1.4 为批量事务恢复增加红灯测试，覆盖暂存失败、部分版本提升失败、计划提交前中断和提交后中断

## 2. 实现基座依赖计划

- [x] 2.1 把已校验仓库包整理为带来源与摘要的候选索引，并扩展依赖求解器支持只读预览候选
- [x] 2.2 实现最小扰动选择策略和结构化计划条目、权限变化与阻塞诊断
- [x] 2.3 实现绑定模块会话、目录 grant、仓库摘要和基线计划代次的短期不透明 `planId` 存储及生命周期清理
- [x] 2.4 实现预览命令，确保请求只接受调用者自己的仓库 grant 与顶层目标 `.mtp` 文件名

## 3. 实现事务式批量安装

- [x] 3.1 实现执行前包摘要、完整包校验、grant 和全局计划代次重新验证，并对变化返回 `stale_plan`
- [x] 3.2 实现单事务暂存、不可变版本目录提升、全局计划最终提交和仅删除本事务新建内容的失败回滚
- [x] 3.3 实现最小事务记录与启动恢复，使提交前中断回滚、提交后中断只完成清理
- [x] 3.4 复用现有权限等待、激活失败恢复和结构化操作日志，不在批量安装中创建或扩大权限批准

## 4. 暴露 Host SDK V6

- [x] 4.1 扩展 Rust 与 TypeScript 运行时清单、SDK 常量和兼容校验以接受 V6，同时保持 V2–V5 原接口
- [x] 4.2 为模块仓库 SDK 增加双语无关的结构化计划类型、`previewInstallPlan` 与 `executeInstallPlan` 方法
- [x] 4.3 把 V6 命令接入原生会话能力校验，并在模块停用、权限撤销或会话释放时使其计划失效
- [x] 4.4 让现有单包 V5 `scan` 与 `install` 行为继续通过回归测试

## 5. 更新独立模块开发体验

- [x] 5.1 更新 `tauri-module-template` 的 V6 SDK 类型、清单示例和 README，并保留可选择旧 SDK 版本的说明
- [x] 5.2 扩展模板模拟宿主，能够生成成功、阻塞、权限等待和 `stale_plan` 计划结果
- [x] 5.3 为模板计划 API、模拟宿主和确定性 `.mtp` 打包增加关键测试

## 6. 升级本地模块市场

- [x] 6.1 将 `local-module-market` 升级到 Host SDK V6 和相应底座版本范围，改用预览与执行接口
- [x] 6.2 实现计划确认界面，展示目标与依赖模块、版本动作、依赖顺序、权限变化、等待状态和阻塞原因
- [x] 6.3 实现取消、执行成功、部分模块等待和计划过期后重新预览但不自动确认的交互
- [x] 6.4 记录计划预览、用户取消、执行结果和失败日志，继续避免记录真实仓库路径与包内容
- [x] 6.5 增加市场模块关键交互测试并重新生成确定性 `.mtp` 包

## 7. 集成验证与规格收尾

- [x] 7.1 运行基座 `pnpm check`、相关 Rust 测试和 `cargo check --manifest-path src-tauri/Cargo.toml`
- [x] 7.2 运行模块模板与本地市场各自的 `pnpm check`，确认工作区无额外生成文件
- [x] 7.3 使用包含 A→B→C 的真实本地仓库冒烟验证预览、确认、依赖顺序、权限等待、过期计划和事务失败恢复
- [x] 7.4 静态审查实现是否泄露路径、绕过权限、自动安装可选依赖或改变 V2–V5 行为，并补齐发现的关键回归测试
- [x] 7.5 更新 README、AI 模块开发提示、CHANGELOG 和版本号，运行 `openspec validate install-local-module-dependencies --strict`
