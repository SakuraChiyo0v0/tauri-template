## 1. 权限与会话契约

- [x] 1.1 先为原生能力清单规范化、危险范围拒绝和权限指纹编写失败测试
- [x] 1.2 先为首次批准、权限扩大升级等待和权限缩小复用编写失败测试
- [x] 1.3 先为活动会话签发、版本绑定、过期令牌拒绝和撤销清理编写失败测试
- [x] 1.4 实现原生能力声明类型、批准状态存储、权限指纹和会话管理器

## 2. 文件与进程代理

- [x] 2.1 先为模块私有根隔离、路径逃逸、符号链接和原子有界读写编写失败测试
- [x] 2.2 先为用户选择 grant、跨模块 grant 拒绝、持久化和撤销编写失败测试
- [x] 2.3 实现私有文件 API、外部文件/目录 grant 和精确撤销
- [x] 2.4 先为 URL scheme、文件定位、可执行 grant、无 Shell 启动、超时和输出上限编写失败测试
- [x] 2.5 实现受控 opener 和有界前台进程运行器，并在会话失效时终止进行中命令

## 3. Windows 注册表代理

- [x] 3.1 先为 HKCU 范围读写、HKLM 只读、前缀逃逸和危险 hive 拒绝编写 Windows 测试
- [x] 3.2 先为字符串、DWORD、QWORD、二进制、多字符串以及单值删除编写测试
- [x] 3.3 实现 Windows 注册表代理和非 Windows `unsupported_platform` 路径

## 4. 托盘与全局快捷键

- [x] 4.1 先为托盘清单冲突、模块分组、事件归属、状态更新和生命周期清理编写失败测试
- [x] 4.2 实现底座统一托盘、声明式模块菜单和会话事件路由
- [x] 4.3 先为快捷键清单、确定性冲突、禁用、重新绑定和生命周期注销编写失败测试
- [x] 4.4 实现全局快捷键仲裁、用户覆盖持久化和会话事件路由

## 5. Host SDK V3 与安装生命周期

- [x] 5.1 在 SQLite V2 合入后同步分支，并核对 V1/V2 清单、SDK 类型和数据库接口基线
- [x] 5.2 先为 SDK V1/V2/V3 判别、V3 会话创建和未授权模块不激活编写失败测试
- [x] 5.3 扩展前后端清单解析、安装状态和诊断以支持 V3 原生能力与等待权限
- [x] 5.4 实现 Host SDK V3 文件、进程、注册表、托盘和快捷键接口及内部 Tauri API
- [x] 5.5 将停用、回滚、卸载、激活失败和权限撤销接入统一资源清理

## 6. 权限管理与模块模板

- [x] 6.1 先为权限摘要、批准/撤销、文件 grant、快捷键冲突和重新绑定编写前端测试
- [x] 6.2 在模块管理页实现等待权限、权限详情、批准、撤销、grant 和快捷键管理
- [x] 6.3 更新独立 `tauri-module-template` 的 V3 类型、模拟宿主和原生能力示例验证页
- [x] 6.4 更新 README、AI 模块开发指引、权限威胁模型和 Unreleased 变更记录

## 7. 集成、审查与验证

- [x] 7.1 使用真实 V3 `.mtp` 验证安装等待、批准、私有/授权文件、受控进程和权限撤销
- [x] 7.2 在 Windows 真实验证注册表范围、托盘事件、快捷键冲突、重新绑定及停用清理
- [x] 7.3 运行 `pnpm check`、Rust 测试、clippy、Tauri 构建和独立模板检查
- [x] 7.4 对照规格完成安全与兼容性 review，运行 `git diff --check` 和 OpenSpec strict validate
- [x] 7.5 同步 CodeGraph；CLI 确认当前工作树没有可用 `.codegraph` 索引，按仓库规则未自行初始化
